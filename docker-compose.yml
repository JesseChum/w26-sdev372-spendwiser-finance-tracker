services:
  db:
    image: mysql:8
    container_name: spendwiser-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: spendwiser
    volumes:
      - db_data:/var/lib/mysql
      - ./Backend/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -u root -p$${MYSQL_ROOT_PASSWORD}"]
      interval: 2s
      timeout: 10s
      retries: 10
      start_period: 5s
    networks:
      - app-network

  test-db:
    image: mysql:8
    container_name: spendwiser-test-db
    restart: "no"
    environment:
      MYSQL_ROOT_PASSWORD: testpassword
      MYSQL_DATABASE: spendwiser_test
    volumes:
      - ./Backend/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -u root -p$${MYSQL_ROOT_PASSWORD}"]
      interval: 2s
      timeout: 10s
      retries: 10
      start_period: 5s
    networks:
      - app-network

  integration-tests:
    build: ./
    container_name: spendwiser-integration-tests
    ipc: host
    environment:
      DB_HOST: test-db
      DB_USER: root
      DB_PASSWORD: testpassword
      DB_NAME: spendwiser_test
      NODE_ENV: test
    depends_on:
      test-db:
        condition: service_healthy
    restart: "no"
    networks:
      - app-network

  backend:
    build: ./Backend
    container_name: spendwiser-backend
    restart: always
    ports:
      - "3001:3001"
    environment:
      DB_HOST: db
      DB_USER: root
      DB_PASSWORD: root
      DB_NAME: spendwiser
      DB_PORT: 3306
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3001/api/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - app-network

  frontend:
    build: ./Frontend
    container_name: spendwiser-frontend
    restart: always
    ports:
      - "5173:5173"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5173/ || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  db_data:
